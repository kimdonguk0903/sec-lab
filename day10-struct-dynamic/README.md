# Day 10 — 구조체 동적 메모리 관리 실습

## 🧠 학습 목표
- 구조체 포인터와 동적 메모리 할당 이해
- realloc을 이용한 구조체 배열 크기 조정
- 함수에서 구조체 배열을 동적으로 추가/반환
- GDB/Valgrind로 동적 메모리 구조 및 누수 확인

---

## 🧩 오늘 실습 내용

1️⃣ struct-dynamic.c
- 구조체 포인터를 이용한 동적 배열 생성
- add_student() 함수로 배열 크기 증가
- realloc() 사용 시 메모리 재할당 및 안전한 값 복사
- 포인터와 포인터의 포인터(int **p) 개념 적용
- 구조체 배열의 메모리 주소와 값 확인

---

## ✅ 오늘의 정리

| 개념 | 설명 |
|------|------|
| 동적 배열 | malloc/realloc으로 크기 조정 가능 |
| realloc() | 기존 메모리를 늘리거나 줄여 재할당 |
| add_student() | 함수에서 동적 구조체 배열을 안전하게 추가 |

---

## 💬 요약 정리
> 구조체 배열도 동적 할당 가능
> realloc()로 메모리 크기를 늘릴 수 있고, 기존 데이터는 안전하게 유지
> 포인터와 포인터 연산을 활용하면 함수 내에서 배열을 관리 가능
> GDB와 Valgrind로 동적 메모리 구조와 누수를 확인하며 안전하게 관리

