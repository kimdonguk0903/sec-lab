#include <stdio.h>

int main() {
    int i;
    for (i = 0; i < 5; i++) {
        printf("Hello %d\n", i);
    }

    int x = 10;
    if (x > 5) {
        printf("x is greater than 5\n");
    }

    return 0;
}
