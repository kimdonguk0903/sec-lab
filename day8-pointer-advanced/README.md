# Day 8 — 다중 포인터 및 동적 메모리 실습

## 🧠 학습 목표
- 포인터의 포인터(`int **`) 구조 이해  
- 포인터 배열(`char *arr[]`)을 이용한 문자열 접근  
- 2차원 동적 메모리 생성 및 해제 실습  
- GDB/Valgrind로 메모리 구조 및 누수 추적 
---

## 🧩 오늘 실습 내용

1️⃣ double_pointer.c
- int *p는 변수의 주소를 저장
- int **pp는 “포인터의 주소”를 저장
- **pp를 통해 실제 값 접근 가능

---

### 2️⃣ pointer_array.c
- 문자열은 사실 char * 형태의 포인터
- const char *arr[]로 여러 문자열을 배열처럼 저장 가능
- 각 문자열의 주소와 값을 출력

---

### 3️⃣ malloc_2d.c
- 행 포인터 배열과 각 행의 데이터 배열을 따로 malloc()
- 사용 후 free()를 역순으로 해제

---
### 4️⃣ function_2d.c
- 함수에서 int ** 매개변수를 받아 출력
- 함수 안에서도 메모리 구조 동일하게 유지
## ✅ 오늘의 정리
| 개념 | 설명 |
|------|------|
| *p | 변수의 주소값을 참조 |
| **pp | 포인터의 포인터 (주소의 주소) |
| 포인터 배열 | 문자열 리스트를 저장하는 구조 |
| malloc() | 동적 메모리 할당 |
| free() | 동적 메모리 해제 (역순 필수) |
| GDB | 변수/주소 구조 분석 |
| Valgrind | 메모리 누수 검사 도구 |

---

## 💬 요약 정리
> 포인터는 “주소”를 저장하는 변수
> 이중 포인터(**)는 포인터의 주소를 저장
> 문자열과 동적 배열 모두 메모리 주소로 연결됨
> GDB로 포인터의 계층적 구조를 관찰
> Valgrind로 누수 없이 동적 메모리를 관리  
